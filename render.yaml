# Render deployment configuration disabled - using manual settings instead
# Configure directly in Render dashboard:
# Build Command: pip install -r requirements.txt
# Start Command: streamlit run frontend/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true